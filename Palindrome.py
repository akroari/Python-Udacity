#!/usr/bin/python
# Filename: palindrome.py

def is_palindrome(racecar):
	if len(word) <=1:
		return True
	elif first('redevider')==last('redevider'):
		return is_palindrome(middle('redevider'))
	else:
		return False